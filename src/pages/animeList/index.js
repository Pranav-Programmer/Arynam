import React from 'react';
import Grid from '@/components/Grid';

const AnimeList = () => {
  
  return (
    <div>
        <Grid movieCategory={'Anime'} categoryName={'Anime'}/>
    </div>
  );
};

export default AnimeList;
