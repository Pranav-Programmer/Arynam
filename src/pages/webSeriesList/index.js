import React from 'react';
import Grid from '@/components/Grid';

const WebSeriesList = () => {
  
  return (
    <div>
        <Grid movieCategory={'Web-Series'} categoryName={'Web Series'}/>
    </div>
  );
};

export default WebSeriesList;
