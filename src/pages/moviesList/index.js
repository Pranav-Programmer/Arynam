import React from 'react';
import Grid from '@/components/Grid';

const MoviesList = () => {
  
  return (
    <div>
        <Grid movieCategory={'Movies'} categoryName={'Movie'}/>
    </div>
  );
};

export default MoviesList;
